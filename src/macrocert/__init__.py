"""MØD-MacroCert: constraint-certified macrocyclization design.

Layers A-C (spec, generate, kernel) are the certifying core. Layer D
(energetics) is defeasible/advisory. Layer E (report) is presentation.
The independent verifier under macrocert.verifier MUST NOT import any
other macrocert.* submodule or mod itself.
"""
__version__ = "0.1.0"
